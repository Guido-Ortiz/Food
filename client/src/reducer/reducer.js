const initialState = {
    recipes: [],
    allRecipes: [],
    details: null
}

const rootReducer = (state = initialState, action) => {
    switch (action.type) {
        // case value:
            
        //     break;
    
        default:
            return state
            break;
    }
}
export default rootReducer